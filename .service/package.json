{
  "name": "notes",
  "description": "small service for demo",
  "version": "0.0.1",
  "scripts": {
    "start": "nodemon src/index.js",
    "build": "esbuild src/index.js --bundle --outfile=dist/bundle.js --platform=node",
    "clean": "rimraf node_modules",
    "update": "npx --yes update-ruecksichtslos"
  },
  "dependencies": {
    "express": "4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "nodemon": "^3.1.3",
    "rimraf": "^5.0.7"
  }
}
